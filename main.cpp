#include <iostream>
#include <clocale>
#include <cmath>
#include "structures.h"
#include "dialog.h"
#include "function.h"

using namespace std;


int main(int argc, char* argv[]){
    std::setlocale(LC_ALL, "ru_RU.UTF-8");
    // double* x = create_pool_empire(100, 0, 1, 5);
    // double* y = create_pool_empire(100, 0, 1, 2);
    // write_to_file(x, y, 100);
    sprashivalka();

}